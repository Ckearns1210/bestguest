<div class="container">
  <%= form_for [@party, @item], :html => {:class => "form-group"} do |f| %>
    <%= f.text_field :name, :class => "form-control"%><br>
    <%= f.submit class: "btn btn-primary btn-block" %>
  <% end %>
</div>
